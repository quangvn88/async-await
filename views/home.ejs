<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title></title>
        <script type="text/javascript">
            let add = (a, b) => {
                return new Promise((resole, reject) => {
                    setTimeout(() => {
                        if(typeof a != 'number' || typeof b != 'number'){
                            return reject(new Error('Tham so phai la number'));
                        }
                        resole(a + b);
                    }, 2000);
                });
            }

            // let a = add(4, 5)
            //     .then(result => {
            //         console.log('Then thu nhat');
            //         return 'Ket qua: ' + result;
            //     }, err => {
            //         console.log(err + '');
            //         return 'Bi loi';
            //     })
            //     .then( b => console.log('Thanh cong roi!' + b
            //     ));
            // console.log(a);
            // setTimeout(() => console.log(a), 2500);

            //let aPromise = Promise.resolve(5);
            //console.log(aPromise);

            // let aPromise = Promise.resolve(add(4, 5));            
            // console.log(aPromise);
        </script>
    </head>
    <body>
        <h1>Hello world</h1>
    </body>
</html>